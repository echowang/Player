<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="video_info_text">
        <item name="android:textColor">@color/video_info_text_name</item>
        <item name="android:textSize">@dimen/text_size_20</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="video_info_value">
        <item name="android:textColor">@color/video_info_text</item>
        <item name="android:textSize">@dimen/text_size_20</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/view_size_10</item>
        <item name="android:singleLine">true</item>
        <item name="android:ellipsize">middle</item>
    </style>

    <style name="video_controller_bar_button">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">@dimen/view_size_60</item>
        <item name="android:background">@drawable/video_controller_bar_pressed_selector</item>
        <item name="android:padding">@dimen/view_size_10</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:focusable">true</item>
        <!--<item name="android:focusableInTouchMode">true</item>-->
    </style>

</resources>
